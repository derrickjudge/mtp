# Force npm to install platform-specific binaries for all environments
# This ensures Vercel deployment has the right binaries
npm_config_platform=linux
npm_config_arch=x64
# Enable optional dependencies
node-linker=hoisted
